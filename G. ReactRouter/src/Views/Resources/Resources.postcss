.container {
  clear: fix;
  margin-left: var(--component-margin);
  margin-right: var(--component-margin);
  background-color: var(--color-primary);
}

.headerText {
  compose: typeHeader from '../../Screens/config.postcss';
}
